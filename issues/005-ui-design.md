# 005 - UI Design (VSCode + 企业微信布局)

## Background

AgentVM Desktop Client needs a professional, familiar UI layout that supports chat-centric AI interaction.

**Design Philosophy**: Chat is all you need - Secondary Sidebar only appears when absolutely necessary.

**Inspiration**:

- VSCode: Layout structure, Activity Bar, flexible panels
- 企业微信: Chat-first, contact list, clean design

---

## Layout Structure

```
┌──────────────────────────────────────────────────────────────────────────┐
│  AgentVM                                            [_] [□] [×]           │
├────┬─────────────────────┬────────────────────────────────────────────────┤
│    │                     │                                                │
│    │  Primary Sidebar    │              Chat Area                         │
│  A │  (200-300px)        │              (flex-1)                          │
│  C │                     │                                                │
│  T │ ┌─────────────────┐ │  ┌──────────────────────────────────────────┐ │
│  I │ │                 │ │  │                                          │ │
│  V │ │  💬 我的对话     │ │  │  👤 帮我分析这个代码                     │ │
│  I │ │  ─────────────  │ │  │                                          │ │
│  T │ │                 │ │  │  🤖 好的，我来帮你分析...                │ │
│  Y │ │  Sales Agent    │ │  │     这段代码主要做了...                  │ │
│    │ │  10:30 AM       │ │  │                                          │ │
│  B │ │                 │ │  │  👤 能优化吗？                            │ │
│  A │ │  Research       │ │  │                                          │ │
│  R │ │  Yesterday      │ │  │  🤖 可以，有几个优化点：                 │ │
│    │ │                 │ │  │     1. 使用缓存减少重复计算               │ │
│  4 │ │  + New Chat     │ │  │     2. 异步处理提升性能                  │ │
│  8 │ │                 │ │  │                                          │ │
│  p │ └─────────────────┘ │  │     [代码块显示优化后的代码]              │ │
│  x │                     │  │                                          │ │
│    │ 或                   │  │                                          │ │
│    │ ┌─────────────────┐ │  │                                          │ │
│    │ │                 │ │  │                                          │ │
│    │ │  👥 通讯录       │ │  │                                          │ │
│    │ │  ─────────────  │ │  └──────────────────────────────────────────┘ │
│    │ │                 │ │  ┌──────────────────────────────────────────┐ │
│    │ │  📦 Prompts     │ │  │  输入消息...                      [发送]  │ │
│    │ │    sales-v1     │ │  └──────────────────────────────────────────┘ │
│    │ │    research     │ │                                                │
│    │ │                 │ │    ← (Secondary Sidebar 按需显示在右侧)        │
│    │ │  🔧 Tools       │ │                                                │
│    │ │    web-search   │ │                                                │
│    │ │    calculator   │ │                                                │
│    │ │                 │ │                                                │
│    │ │  🤖 Agents      │ │                                                │
│    │ │    assistant    │ │                                                │
│    │ │    translator   │ │                                                │
│    │ └─────────────────┘ │                                                │
│    │                     │                                                │
├────┴─────────────────────┴────────────────────────────────────────────────┤
│  连接状态: 已连接 | 模型: claude-sonnet-4                                  │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 三栏布局详解

### 1. Activity Bar (Left, 48px)

**固定在最左侧，图标式导航**

```
┌────┐
│ 💬 │ ← 消息（对话列表）
├────┤
│ 👥 │ ← 通讯录（AI/资源浏览）
├────┤
│ ⚙️ │ ← 设置
├────┤
│ 📊 │ ← 统计（后期）
└────┘
```

**交互**：

- 点击切换 Primary Sidebar 内容
- 当前选中状态高亮
- Hover 显示 Tooltip

---

### 2. Primary Sidebar (200-300px, resizable)

**根据 Activity Bar 选择显示不同内容**

#### 2.1 消息模式（💬）

```
┌─────────────────────────┐
│  我的对话                │
│  ──────────────────────  │
│                         │
│  [+ New Chat]           │
│                         │
│  ┌───────────────────┐  │
│  │ Sales Agent       │  │ ← 对话项
│  │ 帮我写提案         │  │   - 标题
│  │ 10:30 AM          │  │   - 最后消息预览
│  └───────────────────┘  │   - 时间
│                         │
│  ┌───────────────────┐  │
│  │ Research Task     │  │
│  │ 分析市场趋势       │  │
│  │ Yesterday         │  │
│  └───────────────────┘  │
│                         │
│  ┌───────────────────┐  │
│  │ Code Review       │  │
│  │ 优化算法性能       │  │
│  │ 2 days ago        │  │
│  └───────────────────┘  │
│                         │
└─────────────────────────┘
```

**功能**：

- 点击对话项 → 在 Chat Area 打开
- 新建对话 → 选择 AI 类型
- 支持搜索/筛选

#### 2.2 通讯录模式（👥）

```
┌─────────────────────────┐
│  通讯录                  │
│  ──────────────────────  │
│                         │
│  🔍 Search...           │
│                         │
│  📦 Prompts (5)         │ ← 树形结构，可折叠
│    ├─ sales-v1          │
│    ├─ research          │
│    └─ customer-service  │
│                         │
│  🔧 Tools (3)           │
│    ├─ web-search        │
│    ├─ calculator        │
│    └─ file-reader       │
│                         │
│  🤖 Agents (2)          │
│    ├─ assistant         │
│    └─ translator        │
│                         │
│  🏖️  Sandboxes (1)      │
│    └─ python-env        │
│                         │
│  📚 Knowledge (0)       │
│                         │
└─────────────────────────┘
```

**功能**：

- 浏览所有资源
- 点击资源 → 在 Chat Area 显示详情
- 支持搜索、筛选、标签过滤

#### 2.3 设置模式（⚙️）

```
┌─────────────────────────┐
│  设置                    │
│  ──────────────────────  │
│                         │
│  General                │
│  LLM Configuration      │
│  API Keys               │
│  Storage                │
│  About                  │
│                         │
└─────────────────────────┘
```

---

### 3. Chat Area (Main, flex-1)

**核心交互区域**

```
┌────────────────────────────────────────────────────────────┐
│  Sales Agent                                         [···]  │ ← 对话标题栏
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  👤 帮我写一个销售提案                               │ │
│  │                                                      │ │
│  │  🤖 好的，我来帮你写一个销售提案：                   │ │
│  │                                                      │ │
│  │     # 销售提案                                       │ │
│  │     ## 产品介绍                                      │ │
│  │     ...                                              │ │
│  │                                                      │ │
│  │                                          10:30 AM    │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  👤 能加上竞争对手分析吗？                           │ │
│  │                                                      │ │
│  │  🤖 当然可以，让我补充竞争对手分析部分...            │ │
│  │                                          10:32 AM    │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  👤 [正在输入...]                                    │ │ ← 流式输出
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ...                                                       │
│                                                            │
├────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────┐  │
│  │  输入消息... [支持 Markdown]              [📎] [🎤] [发送]│ │ ← 输入区
│  └────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

**功能**：

- 消息列表（虚拟滚动，支持长对话）
- 流式输出显示
- Markdown 渲染
- 代码块高亮
- 输入框（支持 Ctrl+Enter 发送）
- 附件上传（后期）

---

### 4. Secondary Sidebar (按需显示, 250-400px, resizable)

**只在需要可视化时出现，例如：**

#### 场景 A：AI 在修改文件

```
┌──────────────────────┐
│  文件变更             │
│  ──────────────────   │
│                      │
│  ✏️  src/utils.ts    │
│  ✏️  src/api.ts      │
│  ➕ tests/new.test.ts│
│                      │
│  [查看 Diff]         │
└──────────────────────┘
```

#### 场景 B：AI 在调用工具

```
┌──────────────────────┐
│  工具调用             │
│  ──────────────────   │
│                      │
│  🔧 web-search       │
│     状态: 执行中     │
│     查询: "AI trends"│
│                      │
│  🔧 calculator       │
│     状态: 完成       │
│     结果: 42         │
│                      │
└──────────────────────┘
```

#### 场景 C：AI 在执行任务

```
┌──────────────────────┐
│  任务进度             │
│  ──────────────────   │
│                      │
│  ✅ 分析需求         │
│  ⏳ 编写代码 (30%)   │
│  ⬜ 运行测试         │
│  ⬜ 生成文档         │
│                      │
└──────────────────────┘
```

**显示逻辑**：

```typescript
// 默认隐藏
secondarySidebarVisible = false;

// AI 开始工作时自动展开
onAITaskStart(() => {
  secondarySidebarVisible = true;
});

// 任务完成后可手动关闭
// 或 5 秒后自动收起
```

---

## 响应式尺寸

| 区域              | 宽度      | 可调整           |
| ----------------- | --------- | ---------------- |
| Activity Bar      | 48px      | ❌ 固定          |
| Primary Sidebar   | 200-300px | ✅ 可拖拽        |
| Chat Area         | flex-1    | ✅ 自适应        |
| Secondary Sidebar | 250-400px | ✅ 可拖拽        |
| Status Bar        | 100%      | ❌ 固定高度 24px |

---

## Activity Bar 详细设计

### 图标与切换

```typescript
const activities = [
  {
    id: "chat",
    icon: "💬",
    label: "消息",
    shortcut: "Cmd+1",
    sidebar: "chat-list", // Primary Sidebar 内容
  },
  {
    id: "contacts",
    icon: "👥",
    label: "通讯录",
    shortcut: "Cmd+2",
    sidebar: "resource-tree",
  },
  {
    id: "settings",
    icon: "⚙️",
    label: "设置",
    shortcut: "Cmd+,",
    sidebar: "settings-nav",
  },
];
```

### 状态管理

```typescript
interface UIState {
  activeActivity: "chat" | "contacts" | "settings";
  primarySidebarVisible: boolean;
  primarySidebarWidth: number;
  secondarySidebarVisible: boolean;
  secondarySidebarWidth: number;
}
```

---

## Primary Sidebar 内容映射

| Activity  | Sidebar Content   | Description             |
| --------- | ----------------- | ----------------------- |
| 💬 消息   | **Chat List**     | 对话列表、新建对话      |
| 👥 通讯录 | **Resource Tree** | AI/Prompt/Tool 树形列表 |
| ⚙️ 设置   | **Settings Nav**  | 设置分类导航            |

### Chat List 结构

```typescript
interface ChatListItem {
  chatId: string;
  title: string; // "Sales Agent"
  lastMessage: string; // "帮我写提案"
  timestamp: number; // 10:30 AM
  agentType?: string; // 使用的 AI 类型
  unread?: number; // 未读消息数（后期）
}
```

### Resource Tree 结构

```typescript
interface ResourceCategory {
  type: "prompt" | "tool" | "agent" | "sandbox" | "knowledge";
  icon: string;
  label: string;
  count: number;
  collapsed: boolean;
  items: ResourceItem[];
}

interface ResourceItem {
  id: string;
  name: string;
  description?: string;
  version?: string;
}
```

---

## Chat Area 设计

### Message 结构

```typescript
interface Message {
  id: string;
  role: "user" | "assistant";
  content: string; // Markdown
  timestamp: number;
  streaming?: boolean; // 是否正在流式输出
  attachments?: Attachment[]; // 附件（后期）
}
```

### 消息渲染

````
用户消息：
┌────────────────────────────────────┐
│  👤 帮我写一个函数                  │  ← 右对齐
│                        10:30 AM    │
└────────────────────────────────────┘

AI 消息：
┌────────────────────────────────────┐
│  🤖 好的，我来帮你写：              │  ← 左对齐
│                                    │
│  ```typescript                     │  ← Markdown 渲染
│  function example() {              │  ← 代码高亮
│    return "hello";                 │
│  }                                 │
│  ```                               │
│                        10:30 AM    │
└────────────────────────────────────┘
````

### 输入框

```
┌────────────────────────────────────────────────────┐
│  输入消息...                                        │  ← Textarea
│                                                    │  ← 支持多行
│                                                    │  ← 支持 Markdown
│                                                    │
│  [📎 附件] [🎤 语音]          [Ctrl+Enter 发送]     │
└────────────────────────────────────────────────────┘
```

**快捷键**：

- `Ctrl+Enter` / `Cmd+Enter` - 发送
- `Shift+Enter` - 换行
- `Esc` - 取消

---

## Secondary Sidebar 显示策略

### 核心原则

**Chat is all you need - 只在 Chat 无法表达时才显示 Secondary Sidebar**

### 显示时机

| 场景          | 是否显示  | 内容         |
| ------------- | --------- | ------------ |
| 普通对话      | ❌ 不显示 | Chat 足够    |
| AI 修改文件   | ✅ 显示   | 文件变更列表 |
| AI 调用工具   | ✅ 显示   | 工具调用状态 |
| AI 执行任务   | ✅ 显示   | 任务进度     |
| AI 搜索知识库 | ✅ 显示   | 搜索结果     |

### 自动行为

```typescript
// AI 开始工作时
onEvent("agent_start_task", () => {
  secondarySidebar.show("task-progress");
});

// 任务完成后
onEvent("agent_finish_task", () => {
  // 停留 5 秒让用户看到结果
  setTimeout(() => {
    if (!userInteracted) {
      secondarySidebar.hide();
    }
  }, 5000);
});
```

### Secondary Sidebar 内容类型

```typescript
type SecondarySidebarContent =
  | { type: "task-progress"; data: Task[] }
  | { type: "file-changes"; data: FileChange[] }
  | { type: "tool-calls"; data: ToolCall[] }
  | { type: "search-results"; data: SearchResult[] };
```

---

## 实现优先级

### Phase 1: 基础布局 (MVP)

```
- [ ] Activity Bar (3 个图标：消息/通讯录/设置)
- [ ] Primary Sidebar (消息模式 - 对话列表)
- [ ] Chat Area (消息显示 + 输入框)
- [ ] 基础路由和状态管理
```

**不包含**：

- Secondary Sidebar（后期）
- 文件上传
- 语音输入
- 搜索功能

### Phase 2: 通讯录

```
- [ ] Primary Sidebar (通讯录模式 - 资源树)
- [ ] Resource Tree 组件
- [ ] 资源详情页
```

### Phase 3: 设置

```
- [ ] Settings Sidebar
- [ ] 通用设置
- [ ] LLM 配置
- [ ] 数据存储设置
```

### Phase 4: Secondary Sidebar

```
- [ ] Secondary Sidebar 容器
- [ ] 任务进度组件
- [ ] 文件变更组件
- [ ] 工具调用组件
- [ ] 自动显示/隐藏逻辑
```

---

## 技术实现

### 状态管理

```typescript
// Zustand store
interface UIStore {
  // Activity
  activeActivity: "chat" | "contacts" | "settings";
  setActivity: (activity: string) => void;

  // Sidebars
  primarySidebarWidth: number;
  setPrimarySidebarWidth: (width: number) => void;

  secondarySidebarVisible: boolean;
  secondarySidebarContent: SecondarySidebarContent | null;
  showSecondarySidebar: (content: SecondarySidebarContent) => void;
  hideSecondarySidebar: () => void;

  // Current chat
  activeChatId: string | null;
  setActiveChat: (chatId: string) => void;
}
```

### 组件结构

```
src/components/
├── layout/
│   ├── ActivityBar.tsx           # 左侧图标栏
│   ├── PrimarySidebar.tsx        # 主侧边栏容器
│   ├── ChatArea.tsx              # 聊天区域
│   ├── SecondarySidebar.tsx      # 次要侧边栏（按需）
│   └── StatusBar.tsx             # 状态栏
│
├── chat/
│   ├── ChatList.tsx              # 对话列表
│   ├── ChatItem.tsx              # 对话项
│   ├── MessageList.tsx           # 消息列表
│   ├── Message.tsx               # 单条消息
│   └── MessageInput.tsx          # 输入框
│
├── resources/
│   ├── ResourceTree.tsx          # 资源树
│   ├── ResourceItem.tsx          # 资源项
│   └── ResourceDetail.tsx        # 资源详情
│
└── sidebar/
    ├── TaskProgress.tsx          # 任务进度
    ├── FileChanges.tsx           # 文件变更
    └── ToolCalls.tsx             # 工具调用
```

---

## 样式规范

### 颜色系统

```typescript
const colors = {
  // Activity Bar
  activityBar: {
    bg: "#2C2C2C",
    iconInactive: "#858585",
    iconActive: "#FFFFFF",
    iconHover: "#CCCCCC",
    border: "#1E1E1E",
  },

  // Primary Sidebar
  sidebar: {
    bg: "#252526",
    text: "#CCCCCC",
    textActive: "#FFFFFF",
    border: "#1E1E1E",
    itemHover: "#2A2D2E",
    itemActive: "#37373D",
  },

  // Chat Area
  chat: {
    bg: "#1E1E1E",
    userBubble: "#0E639C",
    aiBubble: "#2D2D30",
    text: "#D4D4D4",
    border: "#3C3C3C",
  },

  // Status Bar
  statusBar: {
    bg: "#007ACC",
    text: "#FFFFFF",
  },
};
```

### 间距规范

```
padding: 8px / 12px / 16px / 24px
margin: 8px / 12px / 16px
border-radius: 4px / 8px
```

---

## 参考

### VSCode

- Activity Bar 图标设计
- Panel 拖拽调整大小
- 快捷键体系

### 企业微信

- 对话列表样式
- 消息气泡设计
- 联系人树形结构

### Claude Desktop

- Chat 交互体验
- 流式输出效果
- Markdown 渲染

---

## Acceptance Criteria

- [ ] Activity Bar 可切换（消息/通讯录/设置）
- [ ] Primary Sidebar 显示对应内容
- [ ] Chat Area 可以发送消息并看到回复
- [ ] Primary Sidebar 可拖拽调整宽度
- [ ] 消息支持 Markdown 渲染
- [ ] 代码块支持语法高亮
- [ ] 流式输出效果流畅
- [ ] 支持键盘快捷键

---

**Status**: Open
**Priority**: High
**Labels**: ui, desktop, design
